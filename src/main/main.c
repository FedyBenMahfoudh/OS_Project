#include <stdio.h>

int main(int argc, char *argv[]) {
    printf("Linux Multi-Tasks Scheduler starting...\n");
    
    // 1. Parse the config file from argv[1]
    // 2. Initialize the scheduler engine
    // 3. Run the simulation
    // 4. Print the results

    printf("Scheduler finished.\n");
    
    return 0;
}