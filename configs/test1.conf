# Test configuration file for Linux Multi-Tasks Scheduler
# This file contains sample processes for testing various scheduling algorithms

process P1 {
    arrival_time = 0        # First process arrives immediately
    burst_time = 5         # Needs 5 time units
    priority = 3           # Lower priority (higher number = higher priority)
}

process P2 {
    arrival_time = 2        # Arrives after P1 starts
    burst_time = 8         # Longer execution time
    priority = 1           # Higher priority
}

process P3 {
    arrival_time = 4        # Arrives later
    burst_time = 2         # Short execution time
    # No priority specified - will use default (0 = highest)
}

"""
This is a multiline comment that demonstrates
the parser's ability to handle block comments.
It can span multiple lines and should be
completely ignored during parsing.
"""

process P4 {
    arrival_time = 6
    burst_time = 3
    priority = 2
}

# Test case for same arrival time
process P5 {
    arrival_time = 6        # Same time as P4
    burst_time = 4
    priority = 1
}

# Edge case: zero burst time (should be handled gracefully)
process P6 {
    arrival_time = 8
    burst_time = 3
    priority = 0
}
